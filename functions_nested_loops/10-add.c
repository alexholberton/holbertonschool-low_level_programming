#include "main.h"
#include <stdio.h>
/**
* add - adds two intergers and returns the result
* @a : first interger
* @b : second interger
* Return: the result of the addition
*/
int add(int a, int b)
{
	return (a + b);
}
